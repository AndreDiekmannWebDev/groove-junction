// import { useState } from 'react';
function Home() {
  // const [count, setCount] = useState(0)

  return (
    <div className="page-container">
      {/* <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
        </button> */}
      <h1>Jazz Blues Rock</h1>
    </div>
  );
}

export default Home;
